<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LibraryApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textColor">@color/black</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LibraryApp" parent="Base.Theme.LibraryApp" />

    <style name="SearchButtonStyle" parent="Widget.Material3.MaterialTimePicker.ImageButton">
        <item name="android:color">@color/gold</item>
    </style>
</resources>